---
name: wechat-group-expert-course
description: 把微信群聊记录提炼成「专家课」课程体。第一步先询问用户要导出哪些群的聊天记录以及时间范围，再走后续导出与提炼流程。当用户说"做专家课""群聊转课程""把群聊提炼成课"时使用。
agent_created: true
---

# 微信群聊 → 专家课 提炼

## 第一步：确认导出范围

向用户询问并确认以下两项，拿到后再进入下一步：

1. **要导出哪些群的聊天记录**（群名，支持多个）
2. **时间范围**（起始日期 ~ 结束日期）

确认无误后，调用 `wechat-group-batch-export` 导出对应群在对应时间范围内的原始聊天记录。

## 第二步：导出原始聊天记录

使用 `wechat-group-batch-export` skill 导出第一步确认好的群与时间范围，得到每个群的原始聊天记录（`<群名>/messages.json` + `index.html`）。导出完成后进入第三步。

## 第三步：建立人设并提炼精华（逐群串行，一次只跑一篇）

> ⚠️ **关键约束：多个群时，不能同时给所有群都建立人设并一起跑。** 必须"单群单跑"：建立一个群的人设 → 跑该群对应的那一篇原始聊天记录 → 产出这一篇 → 再建下一个群的人设 → 跑下一篇……严格串行，一篇一篇来。

读取导出的聊天记录，**逐个群**地建立「专家人设」。每处理一个群，完整走完下面流程后再进入下一个群：

**人设提示词（核心）：**

> 你作为一名其对应的相关类型专家的头脑和老师，把其中的精华部分总结提炼出来；内容要让作为学生的我看得懂，且一定不要漏掉任何需要记忆下来的知识点。

执行要点（针对当前这一个群）：
- 先判断该群聊对应的领域/类型（如 AI 视频带货、热词游戏站、SEO 出海等），以此确定「对应类型专家」的身份。
- 以"该领域专家 + 老师"的双重视角来组织内容：既要有专家的深度，又要用老师教学生的口吻讲清楚。
- 提炼时**只留精华**（方法、工作流、工具、数据、案例、踩坑、资源），剔除打卡、寒暄、表情、水群。
- **不漏知识点**：凡是学生需要记住、照着做、考试/实操前该背的要点，都要明确标出（对应后续"必记清单"）。
- 语言要让学生能看懂，避免堆术语；必要时用大白话重述。

**串行节奏（多群时务必遵守）：**
1. 取第一个群 → 建立它的人设 → 用上面提示词跑它的原始聊天记录 → 产出这一篇专家课。
2. 该篇产毕，再取第二个群 → 重新建立它的人设 → 跑它的记录 → 产出第二篇。
3. 依此类推，直到所有群各产出一篇，绝不并行、不混跑。

## 第四步：套「标准美观模板」美化成品（双角色闭环）

> 第三步跑完**一个群**，立刻做这一步，只美化这一个群。本质 = 呈现 / 排版层（presentation layer）：不再提炼内容、不跨群汇总，只把第三步的精华 MD 套进固定好看的模板重新写出来。

### 双角色机制（专家写、读者审）
- **生产（专家写）**：以第三步的「领域专家」人设，把该群精华 MD 套进下方标准模板，产出 HTML。
- **验收（读者审）**：切到「普通阅读者」视角审——可读性高不高 / 好不好看 / 界面舒不舒服。不达标就改，达标才定稿。这是闸门，不是形式。

### 标准搭配（已锁定：baoyu grace 原生风，不再改）
- **引擎**：`baoyu-markdown-to-html`（MD → 内联 CSS HTML，微信兼容）。本机无 bun，走 `npx -y bun` 兜底（已实测可跑，v1.3.14）。
  `{baseDir}` = `C:\Users\my\.workbuddy\skills\baoyu-markdown-to-html`
  命令：
  `npx -y bun {baseDir}/scripts/main.ts <md> --theme grace --color #0F4C81 --font-size 17 --font-family sans`
  - 输出：与输入 MD 同目录同名 `.html`（已存在自动备份 `.bak-时间戳`）。
  - 配色：`--color #0F4C81`（baoyu 预设 `blue`）= 参考文件用的「深蓝」。纯白背景、单栏、标题居中深蓝底白字、引用块浅灰 #f7f7f7、表格细线 #dfdfdf、代码红字。
- **视觉标准（已锁定）**：baoyu **grace** 主题 = 纯白底 + 深蓝 #0F4C81 强调 + 标题居中下边框 + 引用块浅灰 + 表格细线 + 代码红字。干净、Apple 文档感（user 指定「参考文件」风格）。
- **不再手写双栏卡片模板**：此前 Apple 双栏版（进度条+右栏TOC+卡片）为过渡方案，已作废归档；进度条/TOC 属可选增强，非标准必需。

### 排版硬规矩（Butterick + NNGroup，grace 原生已满足）
- 结论先行、单段单意；小标题要有信息量；列表化。
- 79% 读者只扫不读 → 每段前两句给结论 / 价值。
- 移动端自适应：grace 单栏天然不横滑。

### 标准模板（baoyu grace 即标准形态）
- 无需手写模板：`npx -y bun ... --theme grace --color #0F4C81` 的输出就是标准成品（与参考文件一致）。
- 兜底：若环境无 bun 且无法 `npx`，可用 `references/标准模板.html`（Apple 双栏版，离线手写）兜底，但非首选。

### 验收闸门（读者视角，逐项过；任一不过回生产改）
1. 一眼找到"这篇讲什么"（标题 + 首段摘要）。
2. 关键词 / 必记 / 踩坑 / 行动 四类信息一眼可辨（grace 用标题色块 + 列表 + 引用块区分）。
3. 手机上看不横滑、不挤（grace 单栏天然满足）。
4. 通读舒服、不累。
（进度条 / 右栏 TOC 为可选增强，非 grace 原生，不强制。）

### 串行呼应
与第三部节奏对齐：一个群第三步跑完 → 立刻第四步美化这一个群 → 定稿 → 再进下一个群。绝不三群一起美化。
